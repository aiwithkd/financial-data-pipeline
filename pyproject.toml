[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "financial-data-pipeline"
version = "1.0.0"
description = "Financial Data Pipeline & Quality Monitor"
requires-python = ">=3.10"
